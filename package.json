{
  "name": "lab3",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "pretest": "mkdir -p test-results",
    "test": "mocha \"test/**/*.js\" --reporter mocha-junit-reporter --reporter-options mochaFile=test-results/junit.xml",
    "build": "node compile.js"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "ganache-cli": "^6.12.2",
    "solc": "^0.8.30",
    "web3": "^1.10.4"
  },
  "devDependencies": {
    "mocha": "^11.7.2",
    "mocha-junit-reporter": "^2.2.1"
  }
}
